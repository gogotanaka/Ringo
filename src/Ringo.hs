module Ringo where
import Ringo.SKICore

-- main :: IO()
-- main = do
--   [path] <- getArgs
--   code <- filter (not.isSpace) <$> readFile path
--   input <- getContents
--   putStr $ eval code input
